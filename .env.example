# Full URL to the API server (include protocol, no trailing slash)
# Simulator:       http://localhost:3000
# Physical device:  http://<YOUR_MAC_LAN_IP>:3000  (run: ipconfig getifaddr en0)
# Production:       https://your-domain.com
EXPO_PUBLIC_API_URL=http://localhost:3000
